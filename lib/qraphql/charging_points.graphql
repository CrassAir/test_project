query ChargingPoints($offset:Int!, $limit:Int!) {
    chargingPoints(offset: $offset, limit: $limit) {
        count
        data {
            cpType
            cpNumber
            power
            statusId
            location {
                title
                address
            }
        }
    }
}